https://ministack.org/docs/iac

`docker run --rm -p 4566:4566 ministackorg/ministack`


----
aws cli:

set temp env vars:
```
export AWS_ACCESS_KEY_ID="test"
export AWS_SECRET_ACCESS_KEY="test"
export AWS_DEFAULT_REGION="us-east-1"
```

could also create a profile:
```
run:~/github-repos/terraform$ aws configure --profile local

inputs for example:

AWS Access Key ID [None]: test
AWS Secret Access Key [None]: test
Default region name [None]: us-east-1
Default output format [None]: json
```

and use `--endpoint-url=http://localhost:4566` flag

~/.bashrc setting:

```
# awslocal: aws CLI with local endpoint and local profile
awslocal() {
  # preserve original quoting/word-splitting for passed args
  local args=("$@")
  aws --endpoint-url=http://localhost:4566 --profile local "${args[@]}"
}
export -f awslocal
```